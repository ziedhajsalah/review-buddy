# The committed prebuilt viewer (plan 016) is generated output, not source.
# Marking it generated keeps it out of PR diffs and language statistics, and
# signals to reviewers: verify the CI freshness check passed, do NOT review the
# artifact bytes line-by-line. Rebuilt deterministically by CI + release.sh.
src/ui/dist/** linguist-generated=true -diff
